import 'package:flutter/material.dart';

class OnBoadingView extends StatelessWidget {
  const OnBoadingView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('OnBoading View'),
      ),
    );
  }
}
